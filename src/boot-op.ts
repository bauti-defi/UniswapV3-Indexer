// !!! @dev optimism seems to not find any uniswap transactions. Needs further debugging.

process.env.CHAIN_ID = '10';

console.info("Starting Optimism squid")

console.info("Optimism does not currently work. Needs further debugging.")

process.exit(1);

export * from './core/main'

